import { StyledMessage } from "./Message.styled";

const Message = (): JSX.Element => {
  return (
    <StyledMessage>But our princess is in another castle... </StyledMessage>
  );
};

export default Message;
